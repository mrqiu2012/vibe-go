# 控制面板

单独端口运行的控制页面，用于一键重启本项目开发服务。

- **端口**: 3991（可通过环境变量 `CONTROL_PORT` 修改）
- **启动**: 在仓库根目录执行 `pnpm run control`
- **页面**: 打开 http://localhost:3991/ ，点击「重启项目」会先结束 3989/3990 上的进程，再在后台启动 `pnpm dev`。
