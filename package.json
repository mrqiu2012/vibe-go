{
  "name": "web-ide-local",
  "private": true,
  "packageManager": "pnpm@10.4.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n protocol,server,web -c auto \"pnpm --filter @web-ide/protocol dev\" \"pnpm --filter @web-ide/server dev\" \"pnpm --filter @web-ide/web dev\"",
    "build": "pnpm -r build",
    "start": "pnpm --filter @web-ide/server start",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
